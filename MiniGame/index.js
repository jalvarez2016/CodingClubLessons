const prompt = require('prompt-sync')();

const name = prompt('What is your name? ');
console.log(`Oh, so your name is ${name}`);

// Using `prompt` you can make a text adventure game
// Don't forget to install your node modules!